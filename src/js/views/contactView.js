import {elements} from './base';
export const getEmail = ()=> elements.emailForm.value;

export const getFName = ()=> elements.Fname.value;

export const getMessage = ()=> elements.userMessage.value;
